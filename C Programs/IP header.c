#include <stdio.h>
#include <string.h>

#define MIN_NUMBER 0
#define MAX_NUMBER 9

void generate_hexadecimal(*hexadecimal[8]);

int main()
{
   char hexadecimal[8];

   generate_hexadecimal(hexadecimal[8]);
}

generate_hexadecimal(hexadecimal[8])
{
   
}